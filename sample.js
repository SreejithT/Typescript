// type annotation - declaring a variable with type
var s = 123;
// s="123"
var k = "hello"; //type inference- from the value it detects the type
// k=100
// let will be converted to var in ES5
function add(x, y) {
    return x + y;
}
add(10, "b");
